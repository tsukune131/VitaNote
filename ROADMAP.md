# SelfCareNote 公開ロードマップ

(2026-07-20: アプリ名を WeightNote → VitaNote に変更。体重だけでなく
血圧・血糖値・服薬・血液検査まで扱うようになり「Weight」では収まらなくなったため。
「Vita(生命・活力)」+Note。以下の記録中、変更前の名称や旧URLへの言及は
当時の記録としてそのまま残す)

(2026-08-01: アプリ名を VitaNote → SelfCareNote に変更。「Vita」は日本の
40〜60代には意味が届かず、何のアプリか名前から分からなかったため。
体重・服薬・検査値をひとまとめにした「自分の体を自分で手入れする」道具という
中身に、セルフケアがそのまま重なる。-Noteは手帳のデザインごと引き継いだ。
Bundle ID `com.tsukune.vitanote`・GitHubリポジトリ名・PagesのURLは
変更していない。表示名だけの改名で、TestFlightの配布は途切れない)

ゴール: App Storeでの一般公開。
方針: **無料でできる改善をすべて先に行い、納得してから課金ステップ(Apple Developer 年99ドル)に進む。**
各フェーズの最後に「チェックポイント」があり、そこで次に進むか判断する。

収益の位置づけ(2026-07-19決定): 稼ぐことは主目的にしない。
「家族のために作ったアプリを公開し、維持費(年99ドル)を回収できたら成功」。

## プロダクト方針(2026-07-19決定、2026-07-20更新)

**ポジショニング: 「薬剤師の資格を持つ開発者がつくった、体重とお薬の記録手帳」**

体重管理×服薬チェックを1冊のノートにまとめる。主なターゲットは
「健診で指導された・服薬しながら体重管理をしている40〜60代」。
体重アプリとお薬手帳アプリを別々に使わされている層への一冊化提案。

- 表記ルール: 「薬剤師の資格を持つ開発者」を使う(「現役薬剤師」は使わない)
- 「お薬手帳」の正式代替は名乗らない(電子お薬手帳の規格の土俵に乗らない)。
  あくまで「服薬チェックのできる体重手帳」
- 薬効・飲み合わせのアドバイス機能は入れない(記録に徹して医療機器該当性を回避)
- 服薬管理は無料側に置く(差別化のコアは全員に見せる)
- HealthKit自動連携・記録リマインダー通知も無料側に置く(2026-07-20決定)。
  「開いたら終わってる」の価値の柱であり、毎日の記録の摩擦をゼロにする/通知で戻ってくる
  =習慣化の根っこなので全員に開放する。稼ぐことが主目的でない(年99ドルの回収が目標)
  以上、無料体験を最高にしてhabitを作るほうが方針に合う
- Proは「健診・生活習慣病の数値もしっかり管理したい人向けの検査記録バンドル」に
  位置づける(2026-07-20決定): 血液検査・検査値(血圧/血糖)の記録。
  機能が1つに絞られる分「Proとは何か」がひと言で説明できるのが利点。
  (通知をProに入れる案・色テーマ案はいずれも2026-07-20に不採用)

競合(あすけん等)の「クラウド+AI+食品DB+広告」路線とは戦わず、逆張りの柱で仕上げる:

1. **完全ローカル** — アカウントなし・広告なし・通信なし。データは端末内のみ。
   App Storeのプライバシーラベルを「データ収集: なし」にすることが最大の訴求点
2. **書く楽しさ** — 方眼紙・朱印スタンプ・日記。「手帳」として売る
   (2026-08-01: 「きょうのひとこと」は取りやめ。読まれないまま「きょう」タブの
   一番目立つ場所を占めていたため、実装ごと削除した)
3. **開いたら終わってる** — HealthKitで歩数自動、体重だけ1タップ。1日の操作を10秒以内に
4. **散歩+ちょっと我慢の伴走** — 「きょうの処方箋」(2026-07-19実装)で、目標までの
   残りを歩数・ご飯換算という具体的な行動に変換。記録を振り返るだけでなく
   「今日何をすればいいか」を教えるコーチとして機能させる
5. **服薬管理**(2026-07-19実装) — 食事セクションでオンにすると、朝昼夕間食それぞれに
   薬のチェックボックスが出る。薬(名前・食前後・対象の食事)は日を跨いで引き継がれる
   マスタとして登録し、1種類ずつ編集・削除できる。週1回(曜日指定)・月1回(日にち指定)の
   薬にも対応し、該当日だけ「その他のお薬」欄に表示される
6. **検査値の記録**(2026-07-20実装、同日3項目に絞り込み→同日中に腹囲を必須化) —
   腹囲はメタボリックシンドローム基準(男性85cm/女性90cm以上)の必須項目のため
   「きょう」タブで常時入力(トグル不要)。血圧・血糖値は引き続き「あなた」タブで
   任意にオン。「あなた」タブの現在値カード・目標カードに腹囲とメタボ基準を表示し、
   「ふりかえり」の腹囲グラフにも基準線を表示。
   (2026-07-20追加統合) 「あなた」目標カードに目標腹囲を統合(体重・体脂肪率と並び、
   差分statも表示)。腹囲リマインダーは月次→週次(曜日指定)に変更。
   「きょう」タブは体重・体脂肪率・腹囲を1カードに統合(どちらか一方だけの
   入力でも保存可)。「ふりかえり」は体重・体脂肪率・腹囲を1タブに統合し、
   摂取カロリーと飲水も1タブ(「摂取カロリー・飲水」)に統合。血圧・血糖値の
   「検査値」タブはどちらかオンの時だけ表示に戻した(腹囲が抜けたため)
   (2026-07-20さらに調整) メタボ基準の表示は削除(「あなた」の現在値カードの
   色分け・目標カードの基準説明文・「ふりかえり」腹囲グラフの基準線をすべて撤去)。
   「ふりかえり」の体重・体脂肪率・腹囲は3グラフ→2グラフに集約:
   体重(左軸kg)と腹囲(右軸cm)を1つの二軸折れ線に統合、体脂肪率は別グラフのまま
7. **血液検査(健康診断)の記録**(2026-07-20実装) — 日々ではなく数か月に一度の
   健診・血液検査結果(HbA1c・LDL・HDL・TG・AST・ALT・γ-GTP・尿酸・eGFR)を
   「あなた」タブで検査日ごとに登録。「ふりかえり」では折れ線グラフではなく
   年で区切った表として振り返れる(表が肥大化しないよう年ごとにカード分割)
8. **リマインダー通知の設定**(2026-07-20実装、設定のみ先行) — 体重(時刻複数指定、
   デフォルト7:00/20:00)・腹囲(毎月の通知日、デフォルト1日)の通知設定を
   「あなた」タブに追加。実際の発火はPWAでは不安定なため、フェーズCの
   ネイティブ化(Capacitor `@capacitor/local-notifications`)まで待つ方針。
   発火用のヘルパー(scheduleWeightReminders/scheduleWaistReminder)は
   src/lib/platform.tsに用意済み、フェーズCで呼び出しを配線するだけの状態。
   仕様(2026-07-29更新): 体重・腹囲とも「その時刻までに記録が無ければ届く」
   条件付き(体重はその日、腹囲は直前の1週間を見る)。OSの繰り返し通知には
   条件判定がないため、繰り返しは使わず単発通知を積む。開かれない日を埋めるため
   体重は7日先・腹囲は8週先まで前もって積み、アプリを開くたびに窓をずらす。
   記録の保存時もrefreshReminders()で貼り直す
9. **「カレンダー」タブ**(2026-07-30実装) — 歩数とメモは「その日1件」を毎日入力するより
   1か月を見渡して書き足すほうが自然だったため、日にちが行になった月の表を独立タブにした。
   1行 = 日付・1日合計歩数・メモ。歩数のセルをタップするとシートで時間帯別を見られ、
   24個の入力欄が「きょう」タブを圧迫しなくなった。メモのセルは1行プレビューで、
   タップするとシートいっぱいの罫線ページで書ける。あわせて「きょう」タブから
   歩数・メモのセクションを撤去(入力口を1か所に集約)。
   タブは あなた/きょう/カレンダー/ふりかえり/設定 の5つ
   (2026-07-30 同日調整) 時間帯別は手入力の24欄をやめ、「ふりかえり」の時間帯別
   棒グラフを共通コンポーネント(components/HourlyStepsChart)にして両画面で共用。
   内訳はヘルスケア連携から入るものと位置づけ、手入力は1日合計だけにした
   (内訳があるときは合計を編集させない。食い違いを作らないため)。
   メモは未来の日付にも書けるようにし(通院・予定のおぼえ書き)、月送りの制限も外した。
   歩数は当日までのまま。空のメモ欄は「＋メモ」ではなく記入欄の罫線1本で示す

**収益モデル: 基本無料+買い切り¥500「SelfCareNote Pro」**(2026-07-20に線引き更新)
- 無料: 記録の全機能(体重・食事・歩数・貯金・日記)+**服薬管理**+
  **HealthKit自動連携**(歩数取得・体重書き戻し)+**記録リマインダー通知**。
  差別化コア・毎日の摩擦ゼロ・習慣化の仕掛けは全員に開放
- Pro: **血液検査・検査値(血圧/血糖)の記録**。
  「しっかり健康管理したい人向けの検査記録バンドル」
- **広告は入れない**(広告SDKのトラッキングが「データ収集なし」の売りを自壊させるため)

**やらないこと**: 食品DB検索、AI食事解析、SNS共有、家族共有、クラウド同期。
シンプルさ最優先。要望が出ても足さない。

**ビルド環境**: Macは購入しない。GitHub Actionsの標準macOSランナー
(公開リポジトリは無料)でビルド→TestFlightアップロードまで自動化する。
Windowsでの開発+実機確認はTestFlight配布で行う。Xcode 26要件はランナー側で対応済み。

---

## フェーズA: アプリ品質の底上げ(費用: 0円)

PWAのまま、毎日使って気持ちいいレベルまで磨く。

- [x] A-1 食事入力の補助 (2026-07-19)
  - マイメニュー登録(よく食べる物を名前+kcalで保存、ワンタップ入力)
  - よく使う順に自動で並ぶ(使用回数を記録)
- [x] A-2 継続を促す仕掛け (2026-07-19)
  - 連続記録日数(ストリーク)表示
  - 直近7日間のサマリー(貯金合計・体重変化・目標達成日数)
- [x] A-3 オンボーディング (2026-07-19)
  - 初回起動時の案内フロー(ようこそ→プロフィール作成→目標設定→使い方ガイド)
- [x] A-4 パフォーマンス改善 (2026-07-19)
  - 推移画面(Recharts)を遅延読み込みに分割。メインバンドル 715KB→319KB
- [x] A-5 デザイン刷新 (2026-07-19)
  - 「手帳」デザイン採用(方眼紙+朱印スタンプ+進捗リング)。タブ名を あなた/きょう/ふりかえり に変更。アイコン・PWAカラーも刷新

**チェックポイントA**: 家族でPWAを1〜2週間使い、毎日の記録が苦にならないか確認。

## フェーズB: 公開に必要な土台(費用: 0円)

お金をかけずに作れる「ストア品質の裏側」を先に済ませる。

- [x] B-1 クラウド同期 (2026-07-19実装 → 2026-07-19撤去)
  - Googleログイン+Firestore双方向同期を実装したが、方針転換で撤去。
    理由: (1)開発者がFirestore上のデータを閲覧できてしまう構造への懸念、
    (2)歩数連携はフェーズC(HealthKit直接連携)で解決でき同期不要と判明。
    アプリはローカル(Dexie)のみのシンプルな構成に戻した。
    複数端末での利用はフェーズCでのiCloud/端末バックアップに委ねる。
    家族共有(見せ合い)も不要という結論
- [x] B-2 Capacitorプロジェクト化 (2026-07-19)
  - iOSプロジェクト生成(com.tsukune.weightnote)、capacitorビルドモード、通知/HealthKitの差し込み口
- [x] B-3 法務文書 (2026-07-19)
  - プライバシーポリシー・利用規約を公開、設定タブとオンボーディングからリンク
- [x] B-4 エラー監視 (2026-07-19実装 → 2026-08-01撤去)
  - Sentryを導入したが、DSNを一度も設定しないまま撤去した。有効にすると外部通信が
    発生し、プライバシーラベルに「診断情報→クラッシュデータ」の収集申告が要る。
    柱1(通信なし・データ収集なし)と引き換えにする価値はないと判断。
    クラッシュはApp Store Connect/TestFlightのクラッシュレポートで見る

**チェックポイントB**: 同期込みのPWAで納得できるか確認。
**ここが課金判断ポイント** — 納得したらApple Developer登録(年99ドル)へ。

## フェーズC: TestFlight配布(費用: 年99ドル)

- [x] C-1 Apple Developer Program登録 (2026-07-25完了)
- [x] C-2 GitHub ActionsでiOSビルド+TestFlight自動アップロードのパイプライン構築 (2026-07-25完了)
      (公開リポジトリのmacOSランナーは無料。証明書・プロビジョニングはfastlane管理)
      Macを1台も使わずにWindowsだけでTestFlight配布まで到達。ビルドは約3分
  - [x] CI側の実装 (2026-07-25) — .github/workflows/ios-testflight.yml(手動実行、
        lane=beta/certificates を選択)、fastlane/{Appfile,Matchfile,Fastfile}、Gemfile。
        `npm ci && npm run build && npx cap sync ios` → match で署名 → build_app →
        upload_to_testflight。ビルド番号はTestFlightの最新+1(初回はActionsのrun number)
  - [x] Bundle IDの不一致を解消 (2026-07-25) — Xcode側が旧 com.tsukune.weightnote の
        ままだったため com.tsukune.vitanote に統一。Info.plistの表示名・
        ios/App/App/capacitor.config.json も VitaNote に更新
  - [x] 共有スキーム App.xcscheme を追加 (2026-07-25) — 未共有だとCIから
        `-scheme App` でビルドできないため
  - [x] ユーザー作業 (2026-07-25) — App ID作成 / App Store Connectにアプリ登録 /
        App Store Connect APIキー(Admin権限)発行 / match用のプライベートリポジトリ
        VitaNote-certificates 作成 / GitHub Secrets登録(ASC_KEY_ID, ASC_ISSUER_ID,
        ASC_KEY_P8_BASE64, APPLE_TEAM_ID, MATCH_GIT_URL, MATCH_PASSWORD,
        MATCH_GIT_BASIC_AUTHORIZATION)
        → 手順は docs/ios-release-setup.md(手元のみ・リポジトリには入れない)
  - [x] lane=certificates で証明書生成 → lane=beta で初回アップロード成功 (2026-07-25)
  - [x] Node 20 廃止に伴い checkout/setup-node/upload-artifact を v7 に更新 (2026-07-25)
  - [x] 実機で真っ白になる不具合を修正 (2026-07-25) — ワークフローが素の `npm run build`
        を使っており、GitHub Pages用の `base: '/VitaNote/'` でビルドされていた。
        WKWebViewからは全アセットが404になる。`npm run build:capacitor`
        (`vite build --mode capacitor` = 相対パス+SWなし)に変更
- [x] C-3 HealthKit連携(プラグイン: @capgo/capacitor-health) (2026-07-25完了)
      - 起動時に歩数を自動取り込み(時間帯別も)
      - 記録した体重・体脂肪率をヘルスケアへ書き戻し
  - [x] 既定でオンに変更 (2026-07-25) — profile.syncHealthが未設定なら連携オン扱い
        (アプリ版のみ。Webは常にオフ)。オンボーディング終了後の初回起動で
        自動的にヘルスケアの許可を求め、そのままオンとして確定する。
        明示的にオフにしたときだけfalseを保存する
  - [x] 実装 (2026-07-25) — src/lib/health.ts にラッパー。「あなた」タブの
        「ヘルスケア連携」トグル(profile.syncHealth)でオンにすると、起動時と
        前面復帰のたびに直近7日ぶんの歩数を1時間バケットで取り込む。
        今日はヘルスケアを正として毎回上書き、前日以前は記録の無い日だけ埋める
        (手入力を消さないため)。体重保存時に体重・体脂肪率を書き戻す
        (体脂肪率はHealthKitのpercentが0〜1の割合なので/100して渡す)
  - [x] iOS側 (2026-07-25) — App.entitlements(com.apple.developer.healthkit)、
        CODE_SIGN_ENTITLEMENTS、Info.plistにNSHealthShare/UpdateUsageDescription
  - [ ] ユーザー作業: Apple DeveloperのApp IDでHealthKitをオンにする →
        lane=refresh_profiles でプロファイル作り直し → lane=beta
  - [~] 実機で連携が無反応になる問題への対処 (2026-07-25) — 症状は
        `Capacitor.isPluginAvailable('Health')` はtrue(=ネイティブ側は登録済み)なのに
        後続が返ってこず、5秒の打ち切りに落ちる、というもの。
        プラグインを動的import(遅延読み込み)していたのをやめ、静的importにした。
        あわせて@capacitor/local-notificationsも静的importにした。
        **注意: 「WKWebViewで動的importが壊れている」わけではない。**
        「ふりかえり」タブ(React.lazy)は実機で正常に開けることを確認済み(2026-07-25)。
        固まっていたのはプラグインのチャンクに限った話で、真因は未特定。
        静的importで経路自体を無くしたので再発はしないはずだが、
        効いたかどうかは次のTestFlightビルドで要確認
  - [x] 実機確認OK (2026-07-25) — 起動時の許可シート・歩数の取り込み・
        体重の書き戻しを実機で確認。静的importへの変更で無反応の問題も解消した
- [ ] C-4 ローカル通知(記録リマインダー)の実機有効化+ネイティブの細部
      (スプラッシュ・ステータスバー色・触覚フィードバック)
  - [x] 通知の配線 (2026-07-25) — src/lib/notifications.ts に集約(platform.tsは
        isNativeAppだけの薄いモジュールに戻した)。App.tsxが設定値の変化と
        起動・前面復帰を見て通知を貼り直す。設定画面は値の保存と許可の要求のみ。
        体重通知は1件目も含めて日付ごとの単発通知として開くたびに積み直す
        (OSの繰り返し通知に条件判定が無いため)。体重保存時に当日ぶんを取り消す。
  - [x] 1件目も条件付きに変更 (2026-07-29) — 1件目の時刻までに体重が入っていれば
        鳴らさない。開かない日に無音にならないよう、7日先ぶんまで前もって積み、
        開くたびに窓を先へずらす。**既知の割り切り**: 7日以上アプリを開かないと
        以降は届かなくなる(その頃には通知の効き目も薄いと判断)
  - [x] 腹囲も条件付きに変更 (2026-07-29) — 通知時刻(指定曜日9:00)までの1週間に
        腹囲の記録があれば鳴らさない。体重と同じ「単発を先まで積む」方式に揃え、
        8週先ぶんまで積む。「入力済みなら鳴らない」で通知の意味を統一するため。
        設定・起動・記録保存のいずれからも同じ経路を通るよう、DBを読んで貼り直す
        refreshReminders()をsrc/lib/reminderSync.tsに置いた(iOSの64件枠は
        notifications.tsのSCHEDULE_BUDGETで管理し、腹囲を積んだ残りを体重に配る)
  - [x] スプラッシュ (2026-07-25) — @capacitor/splash-screen。描画後にJSから閉じる。
        launchAutoHideは切らない(JSが動かないときに出たままにならないための保険)
  - [x] 触覚フィードバック (2026-07-25) — @capacitor/haptics。服薬チェックと
        飲水ボタンのように「1タップで記録が増える」操作にだけ軽い振動を返す
  - [x] ステータスバー色: プラグイン不要と判断 (2026-07-25) — アプリの配色が
        prefers-color-schemeでOSの明暗に追従しており、iOSの既定のステータスバーも
        同じく追従するため、常に文字が読める。@capacitor/status-barは入れない
  - [ ] 実機確認: 通知が指定時刻に届くか、2件目以降が条件どおりか、
        スプラッシュ・触覚フィードバック
- [ ] C-5 TestFlightで家族に配布、実機フィードバック反映

**チェックポイントC**: 市販アプリと比べて遜色ないか。家族の継続利用で判断。

## フェーズD: App Store公開(追加費用: 0円)

- [ ] D-0 買い切りIAP「SelfCareNote Pro」(¥500)実装
      - 無料: 記録全般+服薬管理+HealthKit自動連携+記録リマインダー通知
      - Pro: 血液検査・検査値(血圧/血糖)の記録
  - [x] 前提(ユーザー作業): 有料App契約・銀行口座・税務情報の登録 (2026-08-01完了)
        手順は docs/appstore-paid-setup.md(手元のみ・リポジトリには入れない)。
        未確認で残っているもの: App Store上の販売者名(個人アカウントは本名)、
        勤務先の副業規定
  - [x] アプリ側の実装 (2026-08-01) — プラグインは @capgo/native-purchases
        (StoreKit 2を直接叩く。RevenueCatは購入情報が第三者のサーバーを経由し、
        「データ収集: なし」が崩れるため不採用)。製品IDは com.tsukune.vitanote.pro。
        src/lib/pro.tsx が判定・購入・復元を持ち、ProProviderでアプリ全体に配る。
        StoreKitに聞けなかったときのために最後の状態をsettingsに残し、圏外でも
        購入済みの記録が読めるようにしている(失敗時にfalseへ倒さない)
  - [x] 無料版の見せ方 (2026-08-01) — 「見えるが書けない」。血液検査・検査値の
        カードは無料でも表示し、入力欄だけをProLockで覆う。過去に記録した結果は
        無料でも読める・消せる(自分の検査結果を人質に取らない)。編集は書き込み側
  - [ ] ユーザー作業: App Store ConnectでIAPアイテム(非消耗型)を作成 →
        Sandboxテスターで購入・復元を確認 → 家族にはプロモコードを配る
  - [x] PWA版(GitHub Pages)の配信停止 (2026-08-01) — ブラウザでは課金できず
        Pro機能の抜け道になるため、iOS版のみのリリースにした。ただしApp Store
        Connectが到達可能なプライバシーポリシーURLを要求するので、Pagesには
        紹介1枚(web/index.html)と法務ページだけを置く。deploy.ymlはこの2つを
        組み立てて配り、型チェックとテストを回すCIも兼ねる。
        vite-plugin-pwaと `--mode capacitor` の切り替えは不要になったので撤去し、
        ビルドは `npm run build`(相対パス・SWなし)の1本に統一。
        紹介ページには古いService Workerを剥がすスクリプトを入れてある
        (残っているとブラウザ版がキャッシュから開き続けるため)
- [~] D-1 ストア素材(スクリーンショット、説明文、キーワード)
      - 訴求軸: 「薬剤師の資格を持つ開発者がつくった」「体重とお薬をこの1冊で」
        「データはあなたのiPhoneの中だけ」「書く楽しさの体重手帳」
  - [x] テキスト一式 (2026-08-01) — store/appstore-ja.md に集約。名前・サブタイトル・
        プロモーション文・説明文・キーワード・審査メモ。文字数は上限内を確認済み
  - [ ] スクリーンショット5枚(実機で撮影。構図とキャプションは同ファイルに記載)
  - [ ] サポート用の問い合わせ先を決めて紹介ページに載せる
- [x] D-2 プライバシーラベル申告(2026-08-01完了) — 「データを収集していません」で
      申告。根拠(通信なし・解析なし・HealthKitは端末内で完結・決済はAppleが扱う)は
      store/appstore-ja.md に記録。依存を足すときはこの申告に戻ること
- [ ] D-3 審査提出(リジェクト往復1〜2回を想定)
- [ ] D-4 公開後の運用体制(クラッシュ監視、iOSアップデート追従)

---

## 費用まとめ

| フェーズ | 費用 |
|---|---|
| A・B | 0円 |
| C以降 | Apple Developer 年99ドル(約15,000円)のみ |

## 現在地

フェーズA・B完了(2026-07-19)。プロダクト方針・収益モデル・ビルド環境も決定済み(上記)。
2026-07-20にアプリ名をVitaNoteへ変更(GitHubリポジトリ・PagesのURLも移行)。
2026-08-01にアプリ名をSelfCareNoteへ変更(表示名のみ。Bundle ID・リポジトリ名・
PagesのURLは据え置き)。App Store Connect側のアプリ名は手作業で変更する。
2026-07-25にC-1(Apple Developer登録)・C-2(TestFlight配布パイプライン)完了。
Bundle ID: **com.tsukune.vitanote**。Macなしで、GitHub Actionsから
TestFlightへビルドを送れる状態になった(Actions → iOS TestFlight → lane=beta)。
フェーズCは実機確認まで完了。2026-08-01にD-0(買い切りPro)のアプリ側を実装し、
有料App契約とApp内課金アイテムの登録も完了。
**次のアクション: TestFlightで購入・復元をSandboxで確認 → D-1 ストア素材**。
紹介ページ: https://tsukune131.github.io/VitaNote/
(2026-08-01にブラウザ版の配信は終了。同URLは紹介1枚と法務ページだけを配る)
(Firebaseプロジェクト weightnote-923c3 は未使用のまま残置。再利用しないなら削除してよい)
